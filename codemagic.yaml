workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      node: 20
    scripts:
      - name: Install dependencies
        script: |
          npm install --legacy-peer-deps
      - name: Build Android APK
        script: |
          # 1. Setup environnement
          NODE_DIR=$(dirname $(which node))
          export PATH="$NODE_DIR:$PATH"
          
          # 2. Création propre des dossiers
          mkdir -p android/app/src/main/assets
          
          # 3. Génération MANUELLE du bundle (On s'assure qu'il est parfait)
          npx react-native bundle \
            --platform android \
            --dev false \
            --entry-file index.js \
            --bundle-output android/app/src/main/assets/index.android.bundle \
            --assets-dest android/app/src/main/res \
            --reset-cache

          # 4. Compilation APK avec l'ordre de NE PAS rebundler
          cd android
          chmod +x gradlew
          # La variable BUNDLE_COMMAND=echo neutralise la commande interne de Gradle
          ./gradlew assembleRelease \
            -PbundleInRelease=false \
            -PreactNativeNodeExecutableAndArgs="$NODE_DIR/node" \
            --no-daemon
    artifacts:
      - android/app/build/outputs/apk/release/*.apk
    publishing:
      email:
        recipients:
          - ton-email@exemple.com
